const CONFIG = {
    BASE_URL: 'https://parcial-ii-corte-be-tareaquenoested.vercel.app/api/v1', // URL del backend desplegado
    TOKEN_KEY: 'sessionToken', // Llave para almacenar el token en localStorage
    //Credenciales
    CREDENTIALS: {
        email: 'admin@admin.com',
        password: 'admin'
    }
}
